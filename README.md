# Videojocs

Practica RA5 de DigiSos, on aprenem a fer operacions CRUD en fitxer amb llenguatge PHP.

CREATE TABLE videojuegos(
    id bigint unsigned not null  primary key auto_increment,
    nombre varchar(255),
    descripcion varchar(255)
);
